name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Production deploys are manual and explicit: run this workflow from the
  # Actions tab on main. Nothing deploys to production automatically.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npx tsc --noEmit

      - name: Run test type check
        run: npx tsc -p test/tsconfig.json --noEmit

      - name: Run tests
        run: npm test

      # Builds the Worker and resolves the staging config without authenticating
      # or uploading, so it runs on fork PRs with no secrets. Catches config,
      # bundle and toolchain breakage on the PR instead of after merge, since
      # deploy-staging below is push-only and never runs on a PR.
      - name: Validate staging deploy config
        run: npx wrangler deploy --env staging --dry-run

  deploy-staging:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: staging
    permissions:
      contents: read
    # Queue deploys rather than running them concurrently, so rapid merges to
    # main can't race and leave staging on the older of the two commits.
    concurrency:
      group: deploy-staging
      cancel-in-progress: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Deploy to staging
        run: npx wrangler deploy --env staging
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

  # Manual production deploy. Runs only via workflow_dispatch on main, so a
  # merge never ships to production on its own. Records the commit SHA on the
  # deployed version so it is possible to tell what production is running.
  deploy-production:
    runs-on: ubuntu-latest
    needs: test
    if: ${{ github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' }}
    environment: production
    permissions:
      contents: read
    concurrency:
      group: deploy-production
      cancel-in-progress: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Deploy to production
        run: |
          npx wrangler deploy \
            --tag "$GITHUB_SHA" \
            --message "Deployed from main @ ${GITHUB_SHA:0:7} by @${GITHUB_ACTOR}"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

  # Failures on main are otherwise invisible: PR checks stay green because
  # deploy-staging is push-only, so a broken deploy is only ever discovered by
  # someone happening to look at the Actions tab. File an issue instead, and
  # comment on the existing one rather than opening a new issue per failure.
  notify-failure:
    runs-on: ubuntu-latest
    needs: [test, deploy-staging]
    if: ${{ failure() && github.event_name == 'push' }}
    permissions:
      issues: write

    steps:
      - name: Open or update the CI failure issue
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const runUrl = `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`;
            const body = [
              'CI failed on `main`.',
              '',
              `- Run: ${runUrl}`,
              `- Commit: \`${context.sha.slice(0, 7)}\``,
            ].join('\n');

            const { data: open } = await github.rest.issues.listForRepo({
              owner,
              repo,
              state: 'open',
              labels: 'ci-failure',
            });

            if (open.length > 0) {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: open[0].number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner,
                repo,
                title: 'CI failure on main',
                body,
                labels: ['ci-failure'],
              });
            }
